using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Arrow : Battler
{
    // Start is called before the first frame update
    void Awake()
    {
        baseAttack = 10.0f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
